version: '3'

services:
  app:
    image: shadowreaver/crypto-signal:master
    ports:
      - "8887:8887"
    environment:
      - CONFIG_PATH=/app/config/config.yml
    volumes:
      - config-data:/app/config

volumes:
  config-data:
